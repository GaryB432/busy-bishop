import * as index from '../src/index';

test('Should have data available', () => {
  expect(index.TempDataSource).toBeTruthy();
});

// test('Should have messages available', () => {
//   expect(index.messages).toBeTruthy();
// });
