import { popup } from '.';

describe('@busy-bishop/extension module', () => {
  it('should add', () => {
    expect(popup).toBeTruthy();
  });
  // it('should greet', () => {
  //   expect(greet('world')).toEqual('@busy-bishop/extension says: hello to world');
  // });
});
